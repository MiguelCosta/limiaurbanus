@model IEnumerable<LimiaUrbanus.WebSite.Models.Freguesia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Concelho.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Concelho.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FreguesiaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FreguesiaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FreguesiaId })
        </td>
    </tr>
}

</table>
