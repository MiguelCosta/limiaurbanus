@model IEnumerable<LimiaUrbanus.WebSite.Models.Concelho>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Distrito.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Distrito.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConcelhoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConcelhoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConcelhoId })
        </td>
    </tr>
}

</table>
